set PROJ        ..

set sim_dir     $PROJ/msim
set src_dir     $PROJ/src

vlib $sim_dir/work
vmap WORK $sim_dir/work

vlog +acc $src_dir/button_TB.v

vlog +acc $src_dir/button.v


vsim -t 1ns -lib work button_TB
